<p style="color: green"><%= notice %></p>
<%= link_to "Keep Shopping",products_path,class:"button is warning" %>
<%= render(@cart.line_items) %>

<div>
  <%= link_to "Edit cart", edit_cart_path(@cart),class:"btn btn-success" %> |
  <%= link_to "Back to carts", carts_path,class:"btn btn-secondary "%>

  <%# <%= button_to "Destroy cart", @cart, method: :delete,class:"btn btn-danger" %> 

  <%= button_to "Destroy", @cart,method: :delete,class:"btn btn-danger" %>
</div>
<span>Total:</span><%= number_to_currency(@cart.total_price) %>